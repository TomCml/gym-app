{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start FastAPI (dev)",
      "type": "shell",
      "options": { "cwd": "${workspaceFolder}/backend" },
      "command": "${workspaceFolder}/backend/venv/Scripts/python -m uvicorn src.main:app --reload --host 127.0.0.1 --port 8000",
      "problemMatcher": []
    }
  ]
}
